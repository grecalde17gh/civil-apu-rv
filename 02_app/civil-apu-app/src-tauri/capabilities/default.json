{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the isolated desktop prototype.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
